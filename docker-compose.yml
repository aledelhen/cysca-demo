services:
  cysca-mock:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      GAUGE_PYTHON_COMMAND: /app/.venv/bin/python
    volumes:
      - .:/workspace
      - ./reports:/workspace/reports
      - ./logs:/workspace/logs
