# Gauge environment configuration

# Write HTML report output into a predictable project-local directory.
gauge_reports_dir = reports

# Create a fresh timestamped report directory for each run.
overwrite_reports = true

# Keep runtime logs in the project so failures are easy to inspect.
logs_directory = logs
